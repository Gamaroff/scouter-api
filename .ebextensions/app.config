# If you do not specify a namespace, the default used is aws:elasticbeanstalk:application:environment
option_settings:
 - option_name: NODE_ENV
   value: production
container_commands:
  01-command:
    command: "chmod 766 uploads"
